# Normalize line endings: text files stored as LF in the repo.
* text=auto eol=lf

# Windows batch launcher must stay CRLF to run correctly.
*.cmd text eol=crlf
# Treat these as binary (no diff/eol munging).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.mp4 binary
